@model DescountModel
@{
    ViewData["Title"] = "CreateDescount";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<nav class="navbar bg-body-tertiary">
    <div class="container-fluid">
        <h1 class="display-10">Agregar una Descuento </h1>
        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
            <a asp-action="ShowDescounts" asp-controller="Descount" class="btn btn-primary">Volver a la lista</a>
        </div>
    </div>
</nav>
<div class="card">
    <div class="card-body">
        <form asp-action="CreateDescount" asp-controller="Descount" method="post">
            <div class="mb-3">
                <label class="form-label">Codigo</label>
                <input asp-for="Code" type="text" class="form-control">
                <spam asp-validation-for="Code" class="text-danger"></spam>
            </div>
            <div class="mb-3">
                <label class="form-label">Cantidad de descuento para porcentaje</label>
                <input asp-for="Cant" type="text" class="form-control">
                <spam asp-validation-for="Cant" class="text-danger"></spam>
            </div>

            <div class="text-end">
                <button type="submit" class="btn btn-primary">Agregar</button>
            </div>

        </form>
    </div>
</div>

